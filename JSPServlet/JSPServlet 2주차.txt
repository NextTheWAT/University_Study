JSPServlet 2주차

3장

JSP 파일의 변환
네트워크를 통해서 서버에 요청 jsp페이지를
서버에서는 jsp컨테이너에 사용자가 요청한 페이지가 있는지 검색
있다면 그 페이지를 돌려주는거고 없으면 404가 나온다

서블릿으로 변환을해서 jsp_java로 변환이 된다
그리고 0과1로 이루어져있는 class파일 컴퓨터가 이해할수있는걸로
변환해준다

그리고 메모리에 적재해서 실행을 시킨다
실행된 결과를 HTML로 서버에서 사용자한테 돌려준다

jsp -> java -> class

과정 중요@
1. JSP 페이지 요청
2. JSP 컨테이너로 전송
3. JSP 파일 파싱
4. 서블릿으로 변환
5. 클래스 파일 생성
6. 메모리에 적재되어 실행됨
7. HTML로 전송
8. HTML 표시(실행)


init() 메서드
- 처리에 필요한 자원에 대한 초기화 작업

service() 메서드
- 요청에 대해서 필요한 처리
- 실제 요청에 대한 처리를 담당하는 메서드로
  필요한 로직이 담겨있는 메서드

destroy() 메서드
- 사용된 자원을 해제한 뒤, 서블릿을 종료


서블릿이란?
 JSP 이전 세대의 동적 웹페이지 생성 언어
 Server + Applet의 합성어 -> Servlet
 브라우저에서 호출하면 WAS에서 웹 페이지를 동적으로 생성


JSP 파일 동작구조@
서블릿 어떻게 작성하는지@
서블릿이 무엇인지@
JSP 이전 세대의 동적 웹페이지 언어


JAVA

JVM이 맨 처음 찾아가는 놈은 main 메서드 이다
new - 인스턴스 생성
static - 미리 메모리 공간 할당
java는 메모리 공간이 언제 할당되는지 알아야함@
String[] args - 아규먼트 지정해서 사용하는것

int a=4;
정수형 변수 4바이트를 메모리공간 할당을 하고
a가 가르키는 메모리 공간에 있는값이 4다


String name = "JS";
name은 참조변수 라고한다 메모리 주소를 참조해서@
name안에 주소값이 있는데 그걸 가르키는 메모리 공간에 JS가 들어가있다


Variable V = new Variable();
V가 가르키는 주소값 안에 Variable의 인스턴스가 있다
V안에 인스턴스에는 name, year, JS가 있다


배열에 특화된 for문 인핸스드 for문
a[] 가 있다면
for(int b : a) 이런식으로


정수는 기본 자료형이 int
실수는 기본 자료형이 double
정수간에 연산은 정수만
7/2는 3.5가 맞지만 3이 나온다 정수끼리 연산을 해서
만약 3.5가 나오게하려면 7/2.0으로 해야한다


논리 연산자
|| - a 또는 b가ture일 경우 ture를 반환 
&& - a 와 b 모두 ture일 경우 ture를 반환
! - a와 반대값을 반환

비트 연산자
| - 
& - 


switch(전달인자)
문자열, 정수, 정수형 상수, 정수형 변수, 정수형 메소드
가 들어올수있다


for(int i=1; i<=10; i++) {
	if(i%2==1) continue;
	System.out.println(i)
이런식으로 컨티뉴를 사용하면 홀수일때는 그냥 넘어간다


클래스 - 붕어빵 틀
객체 - 붕어빵


상속
- 자식클래스가 부모클래스의 구성요소를 상속
생성자 호출해줘야함

static이 붙으면
멤버메소드, 멤버변수가 아니다






