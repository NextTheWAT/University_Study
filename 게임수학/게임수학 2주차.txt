게임수학 2주차

콘솔 앱 (.net.Framework)

컨트롤 + f5

namespace - 클래스들을 모아놓은 묶음이다

using System; //시스템이 없다면 콘솔 앞에 System을 사용해야한다
              //ex System.Console.WriteLine
            //using FirstProject.Starcraft; 이렇게 써주면 FirstProject의 Starcraft의 클래스를 사용한다는 것
            //using System. 을 하게 된다면 다른 클래스들을 이용해서 코딩을 하는것

WriteLine - 메서드

String 은 포인터다

class Zerg z - Zerg 라는 타입을 선언한것, z는 변수 명
항상 타입 - 변수 = 값 으로 된다

new 는 동적 생성자 다

클래스 - 클래스를 통해서 변수를 메모리에다가 할당한것을 객체 라고 부른다
클래스와 객체는 다른것

인스턴스를 객체라고 부른다

객체는 메모리가 할당된것 - 그것은 인스턴스

포인터 - int *i; 는 4바이트씩 띄워지는 변수 - 포인터 배열
포인터는 주소를 알고있음

클래스 - 타입변환을 많이함 그래서 타입 변환을 잘 해줘야한다
		그렇지않으면 사용하기 힘들다

        enum CAL { SUM, SUB, VAL, DIV} //타입 선언 enum은 타입 CAL 은 변수
                                        //CAL은 만든 변수 정수형은 0, 1, 2, 3 이 들어간다
                                        //접근할때는 CAL.SUB등 으로 접근한다
                                        //enum을 게임에서는 굉장히 많이 사용한다 그리고 스위치문 사용 많음@@ 시험 나올 가능성 높음
                                        //이니멀레이션? - enum
//region 코드를 접고 필수있음 - 잘 사용하자


정수형 변수 입력하는 방법
            int i = Int32.Parse(Console.ReadLine());
            int j = Int32.Parse(Console.ReadLine());

문자열 입력하는 방법

op코드 - +할지-할지*할지/할지 정하는것
		0, 1, 2, 3으로

오버로딩 - 매개변수가 다른것 똑같은 이름
오버라이딩 - 기능을 업데이트 해주는 상속의 개념

foreach문 - 타입에 맞는 값을 가져옴
ex)    foreach(int num in al)






