게임수학 5주차

        if (Input.GetKey("right"))  //만약 오른쪽 화살표키가 눌러졌다면
        {
            vx = speed;
            leftFlag = false;
        }

GetKey
GetKeyDown
GetKey
등등이 있다
up, down, left, right 가 있다.

leftFlag = false; //오른쪽이라면 false
ture 라면 // 왼쪽 방향으로

Sprite Rederer에 있는 Flip 이라는 변수의 x값을 눌러주면 왼쪽 오른쪽을 변환할 수 있다.
this.GetComponent<SpriteRenderer>().flipX = leftFlag;
왼쪽으로가면 ture 가 되고, 오른쪽으로가면 false가 된다.


vx, vy 를 나누고

vx는 오른쪽 - speed, 왼쪽 - -speed
vy는 위 - speed, 아래 - -speed


Update - @
FixedUpdate - @

1~2 프레임 사이에 있는게 deltaTime 이다
1초에 60번이 호출이 되는 함수가 Update
Update는 끊길수있다 - ex)팍팍 감
각 컴퓨터의 프레임대로 만들어준다 120인사람은 120프레임
30프레임 사람은 30프레임

FixedUpdate는 시간값이 많으면 빨리가고, 시간값이 적으면 느리게간다
		   그래서 
고정으로 50프레임으로 해준다 - 조금 느리게 감
프레임이 120인 사람이랑 30인 사람이랑
동일하게 만들어준다

Update() - 스크립트가 enabled 상태일때, 매 프레임마다 호출됩니다. 일반적으로 가장 빈번하게 사용되는 함수이며, 
물리 효과가 적용되지 않은 오브젝트의 움직임이나 단순한 타이머, 키 입력을 받을 때 사용됩니다.

FixedUpdate() - 프레임을 기반으로 호출되는 Update 와 달리 Fixed Timestep에 설정된 값에 따라 일정한 간격으로 호출됩니다. 
물리 효과가 적용된(Rigidbody)  오브젝트를 조정할 때 사용됩니다(Update는 불규칙한 호출임으로 물리엔진 충돌검사 등이 제대로 안될 수 있음).

충돌에 대한 조건이 있어야한다


단위벡터@


오늘의 정리@@@ 시험에 나올 수 있음
1. 키보드 처리 GetKey("right")
	enum KEY - switch ~ case (소개를 했었음) OnKeyPress_MoveSprite1 코드에
2. 충돌	2-1 Rigidbody
		2-2 Box Collider2D 다양한 Collider가 있다(충돌체크(Collider))
		2-3 BoxCollider2D 충돌 받는 애도 있어야한다
3. 이동
		3-1 transform (x,y,z) 의 값을 계속 업데이트
		3-2 Rigidbody 물리엔진 힘으로 이동처리	
4. 따라가기
		4-1 방향을 구함
		4-2 방향에 속도를 곱해준다 - 그러면 (x,y) 값을 구해서 따라가기
		4-3 Rigidbody2D를 이용해서 계속해서 업데이트를 해줌 - 이동











