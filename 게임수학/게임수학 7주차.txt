게임수학 7주차

애니메이션
Animator Controller(관리자)
- 애니메이션 클립을 다루는게 애니메이터 컨트롤러다@
- 하나의 컨트롤러에서 애니메이션을 관리해준다(클립)

Animation Clip
- Up, Down, Left, Right
같은 파일들
이 파일들을 모아서 에니메이터 컨트롤러로 만든다
4개의 애니메이션을 만들면 4개의 컨트롤러가 생기는데
1개만 남기고 나머지를 다 딜리트 한다


7장 스크린 좌표계에서 카메라 좌표계로 변환 @@@

Instantiate() @@@
InvokeRepeating()
Destroy()  @@@

오브젝트를 프리팹으로 만들면 프리팹타입이고
프리팹에 있는 건 오브젝트타입으로 변환해서 오브젝트로 사용할 수 있다.
물리적으로 파일에 담겨있다

프리팹 -> 오브젝트
게임오브젝트를 인스턴스화 해주는것이다.


마우스를 클릭했을때 (x,y) 값을 가져온다(스크린 좌표계)
공간상에서 봤을때는 (x,y,z) 이다	(공간좌표계)
좌표를 변환해 줘야한다
스크린좌표게를 카메라 좌표계로 변환해줘야한다.
프로젝션 이라고 부른다@@@
반대는 언프로젝션@

   void Update()
    {
        if (Input.KeyMouseButtonDown(0))    // 왼쪽 마우스가 눌렸을때
        {
            Vector3 pos = Vamera.main.ScreenToWorldPoint(
                Input.mousePosition + Camera.main.transform.forward
                );
            pos.z = -5;
        }
    }
}
(5,5) + (0 ,0 ,1) = (5,5,1)







